<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NavigationTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#24FF43</item>
        <!--<item name="tabTextAppearance">@style/Widget.AppCompat.Toolbar.Button.Navigation</item> -->
        <item name="tabBackground">@color/colorAccent</item>
    </style>

</resources>
